<!--
Exemplar of the project's root README.md (English, the default language). Written in
step 8.5 of SKILL.md, after Verify, with the {{...}} placeholders already resolved from
data step 1-8 already computed — nothing here is invented. Companion file
README.pt-br.md.example is the Portuguese version, cross-linked to this one. Same rule
as § andaime: the top comment stays only for the "why" the *generated project's* README
looks this way; anything describing this file as an exemplar or citing the companion
`.example` is cut when step 8.5 writes the real file.
-->

# {{projectName}}

Spring Boot project generated by [claude-spring-architect](https://github.com/ice-lfernandes/claude-spring-architect),
already prepared for AI-assisted development with Claude Code.

*[Versão em português](README.pt-br.md)*

## Origin

Generated from the `claude-spring-architect` meta-repo, with:

```
{{initCommand}}
```

| Parameter | Value |
|---|---|
| groupId | `{{groupId}}` |
| artifactId | `{{artifactId}}` |
| Project name | `{{projectName}}` |
| Target directory | `{{targetDirectory}}` |

The command ran from the `claude-spring-architect` repository, but this project is
self-contained — nothing here depends on the meta-repo existing on whoever clones it.
See `CLAUDE.md` for the full architecture; this file only orients a first-time reader.

## Blueprint

`{{blueprint.id}}` — {{blueprint.oneLineDescription}}

## Stack and versions

Resolved live by the Spring Initializr at generation time — never pinned from memory:

- Java {{javaVersion}}
- Spring Boot {{springBootVersion}}
- Build: {{buildTool}}

## Active features

{{featuresList}}

## Architecture of `.claude/`

This project carries its own `.claude/`, laid out the same way as the meta-repo that
generated it:

- **Hooks** (`.claude/hooks/ArchHook.java`) enforce deterministically — layer boundaries
  (`.claude/forbidden-imports.txt`), frontmatter, Lombok config. What they check,
  nothing bypasses without editing the hook.
- **Skills** (`.claude/skills/`) hold procedure. Invoke one directly as `/<skill-name>`,
  or describe the task and Claude routes to it.
- **Agents** (`.claude/agents/`) run an isolated step a skill delegates to — for
  context, restricted tools, or a different model.
- **Rules** (`.claude/rules/`) are the leaf: norms cited by skills and agents, never
  invoking anything themselves. Index at `.claude/rules/00-index.md`.

### Skills available

{{skillsList}}

### Agents available

{{agentsList}}

## Generation report

Captured here in the meta-repo, at the end of the run that created this project — not
reconstructed afterward:

```
{{outputContractBlock}}
```

## Next steps

{{nextStepsBlock}}
